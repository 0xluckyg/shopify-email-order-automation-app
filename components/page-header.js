const pageHeader = (text) => {
    return (
        <div style={{marginBottom: '30px'}}>
            <h1 style={{width: '300px', fontSize:'25px'}}><strong>{text}</strong></h1>
        </div>
    )
}

export default pageHeader